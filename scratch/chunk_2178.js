"    const { logs, employees, isDemo, error } = await fetchAttendanceLogs(month);\n\n    // 1.5. 각 출근 로그에 대해 지정된 출근 시간 기준으로 지각 여부(isLate) 주입\n    logs.forEach(log => {\n      if (log.eventType === '출근') {\n        const scheduleTime = (settings.employeeSchedules && settings.employeeSchedules[log.empNo]) || '09:00';\n        const timeOnly = log.logTime.split(' ')[1]; // \"HH:MM:SS\"\n        log.isLate = timeOnly > `${scheduleTime}:00`;\n      } else {\n        log.isLate = false;\n      }\n    });\n\n    // 1. 전체 직원 목록 기준 설정"