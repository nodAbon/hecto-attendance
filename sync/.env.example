# ================================================================
# sync/.env.example  →  서버PC에 .env 파일로 복사 후 값 입력
# ================================================================

# Supabase (서비스 롤 키 - 서버에서만 사용, 절대 공개 금지)
SUPABASE_URL=https://XXXXXXXXXXXXXXXX.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sb_secret_XXXXXXXXXXXXXXXX

# AWS MySQL 세콤 DB (읽기전용)
MYSQL_HOST=Prd-Hecto-WHR-Ext-NLB-XXXXXXXX.elb.ap-northeast-2.amazonaws.com
MYSQL_USER=whradmin
MYSQL_PASSWORD=your_password_here
MYSQL_DATABASE=whr
MYSQL_PORT=3306

# 동기화 설정
SYNC_INTERVAL_MS=180000        # 출입기록 주기 (기본 3분)
LEAVES_SYNC_INTERVAL_MS=600000 # 연차+직원 주기 (기본 10분)
MY_COMPANY_CODE=1600

# 로컬 SQLite 경로 (세콤매니저 설치 경로)
SECOM_DB_PATH=C:\(주)에스원\세콤매니저(근태·식당) v9.0.1\Database
